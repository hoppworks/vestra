# ADR 0002: Relative scale in v1

Status: accepted — 2026-08-13

V1 reconstructs a coherent world at arbitrary relative scale. Metric scale,
NESTED model distribution, measurement anchors, and validation in metres are
deferred. Scene files must declare their scale status explicitly so a future
metric migration cannot reinterpret an old scene silently.
