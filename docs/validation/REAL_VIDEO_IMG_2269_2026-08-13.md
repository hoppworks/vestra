# Real-video relative-world validation — 2026-08-13

This record covers Vestra's first full local reconstruction of a real phone
video. It establishes pipeline and evidence integrity, **not** metric accuracy
or a semantic/mesh reconstruction claim.

## Input and locked environment

| Field | Value |
| --- | --- |
| Input | `IMG_2269.MOV` (local user-provided capture) |
| Duration / source raster | 40.365 seconds / 1920×1080 |
| Selected frames | 120 uniformly sampled RGB24 frames |
| Reconstruction raster | 504×336 |
| Windowing | 12 frames, 3-frame overlap (14 windows) |
| Geometry sampling | confidence threshold `1.0`, pixel stride `8` |
| Model | `depth-anything-base-f32.gguf`, SHA-256 `1b13b166e8a8b4f2c862f42d36edb2f9aab995a18cc527a52b9f160b99c6b8da` |
| Host | AMD Ryzen 9 9950X, `RAYON_NUM_THREADS=16` |
| Release code | Vestra `9a78445`, Engine `1562f8b70a1b35a9908feb88eaa38577b92f2a2a`, Kernels `bde198958348fcb7a0a294e0d05cd8f2f7e93c5b` |
| Code generation | `RUSTFLAGS="-C target-cpu=znver5"` |

## Procedure

```bash
RAYON_NUM_THREADS=16 ./target/release/vestra reconstruct \
  --video IMG_2269.MOV \
  --model depth-anything-base-f32.gguf \
  --output img-2269.vestra

./target/release/vestra inspect --scene img-2269.vestra
```

The first reconstruction checkpointed all 14 immutable measured windows. Its
strict fusion initially rejected the first seam solely because an arbitrary
relative scale lower bound of `0.1` rejected a well-supported scale of
`0.07178083`: 7,938 direct source-pixel correspondences, 7,890 inliers, and
RMS residual `0.010779493`. This was a product bug: relative multiview scale
has no defensible fixed `0.1…10` range. Vestra `9a78445` changed the gate to
numeric-collapse protection (`1e-3…1e3`) while retaining correspondence,
inlier, residual, finite-geometry, and degeneracy checks. A dedicated unit
test covers a supported `0.05` relative seam.

Fusion was then rerun from the persisted raw windows, without repeating model
inference.

## Observed persisted report

```json
{
  "state": "fused_relative_world",
  "capture_quality": {
    "disposition": "ready",
    "frame_count": 120,
    "mean_adjacent_luma_delta": 0.09700285643339157
  },
  "measured_window_count": 14,
  "fused": {
    "points": 390182,
    "progressive_point_chunks": 8,
    "finite_points": true,
    "sequential_alignment_count": 13,
    "sequential_correspondence_count": 103194,
    "sequential_inlier_count": 100940,
    "voxel_size": 0.0013986955164000392,
    "pose_graph": null
  },
  "scale": "relative"
}
```

The Studio HTTP server successfully served the scene manifest and its eight
progressive point chunks. The browser automation container could not reach the
host-local development port, so this record makes no browser-render screenshot
claim. The Studio's local browser rendering path is separately covered by the
earlier end-to-end smoke.

## Interpretation and remaining gate

- All model windows, durable raw evidence, sequential seams, fusion, and
  progressive delivery completed successfully for real video.
- `ready` is only an adjacent-luma capture-risk signal; it is **not** a
  reconstruction-quality certificate.
- The world remains relative scale. No distance or room dimension is claimed.
- No non-adjacent revisit met the conservative automatic loop-closure gates;
  `pose_graph: null` is correct evidence, not a missing data field.
- The next acceptance run should deliberately include a known revisit and be
  inspected for an accepted loop edge, seam diagnostics, holes/ghosting, and
  spatial plausibility in Studio.
